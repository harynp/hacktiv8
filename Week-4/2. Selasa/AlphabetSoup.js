function AlphabetSoup(str) { 

  var reverse = str.split('').sort().join('');  
  
//   for(i=0;i<str.length;i++){
 
//   }
  return reverse; 
         
}
   
// keep this function call here 
AlphabetSoup(readline());